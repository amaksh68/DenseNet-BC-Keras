# DenseNet-BC-Keras
The file main.py implements the DenseNet-BC model introduced by https://github.com/liuzhuang13/DenseNet.
The file ablation.py provides a code that randomely removes a specific ration of the weights inside a layer.
